const issues = [["Cross-agency SITREP template mismatch", "High"],["Medical evacuation timeline exceeded SLA", "High"],["Radio channel discipline dropped during surge", "Medium"],["Unclear ownership on perimeter recon checks", "Medium"]];
const lifecycle = [["Plan",100],["Execute",100],["Review",72],["Improve",38],["Library",46]] as const;

export default function CommandCenterPage() {
  return <section><h1>Command Center</h1><p className="muted">Current exercise: <strong>LIGHTNING STRIKE</strong> • Phase: <strong>Review</strong> • Readiness: <strong>82% Ready with Friction</strong></p><div className="grid" style={{ marginTop: 16 }}><article className="card"><h3>Next Best Action</h3><p className="muted">Approve cross-agency comms correction package and assign POA&amp;M owners before 1700Z.</p></article><article className="card"><h3>Priority issues</h3><ul className="issue-list">{issues.map(([title, severity]) => <li key={title} className="issue-item"><span>{title}</span><span className="badge">{severity}</span></li>)}</ul></article><article className="card"><h3>Lifecycle progress</h3><div className="progress-wrap">{lifecycle.map(([name, value]) => <div key={name} className="progress-row"><span className="muted">{name}</span><div className="progress-bar"><div className="progress-fill" style={{ width: `${value}%` }} /></div></div>)}</div></article></div></section>;
}
